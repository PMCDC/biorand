<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:toggleButtons="clr-namespace:biorand.desktop.Controls.ToggleButtons">
    <Style x:Key="GroupHeaderToggleButton" TargetType="{x:Type toggleButtons:GroupHeaderToggleButton}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type toggleButtons:GroupHeaderToggleButton}">
                    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image x:Name="ToggleImage" Width="16" Source="{StaticResource ToggleGroupCheckedIcon}" />
                            <TextBlock x:Name="ToggleText" Margin="5,0,0,5" Text="{TemplateBinding ToggleText}" FontSize="14" Typography.Capitals="AllSmallCaps" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToggleText" Property="Foreground" Value="White"/>
                            <Setter TargetName="ToggleImage" Property="Source" Value="{StaticResource ToggleGroupCheckedIcon}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="ToggleText" Property="Foreground" Value="DarkGray"/>
                            <Setter TargetName="ToggleImage" Property="Source" Value="{StaticResource ToggleGroupNotCheckedIcon}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SwapToggleButton" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image x:Name="ToggleImage" Width="20" Source="{StaticResource SwapEnabledIcon}" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToggleImage" Property="Source" Value="{StaticResource SwapEnabledIcon}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="ToggleImage" Property="Source" Value="{StaticResource SwapDisabledIcon}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>